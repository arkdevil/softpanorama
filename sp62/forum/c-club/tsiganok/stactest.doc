────────────────────────────────────────────────────────────────────

          ┌────────────────────────────────────────────╖
          │ * Tsyganok Service * 1993 * Тесты MS DOS * ║
          ╘════════════════════════════════════════════╝

────────────────────────────────────────────────────────────────────

            * Tsyganok Service * 1993 * Stacker Test *

     Как можно установить наличие в памяти STACKER диска ?
 Нужно выдать могическую комбинацию :
        .....
        mov     ax,0cdcdh
        lea     bx,bufs
        mov     cx,1
        xor     dx,dx
        int     25h
        ....
bufs    dw      0,0
        dd      0

     Если по возвращению из прерывания int 25h AX не равно CDCDh, 
 значит STACKERа нет. В этом случае не забудьте очистить стек ( pop 
 ax ). 
     Если вы установили наличие STACKER-а в памяти, установите 
 версию программы :
        lea     si,bufs+4
        lds     si,dword ptr [si]
        lodsw
        lodsw
     Теперь AX содержит номер версии STACKER в формате 
      m*100+j, и можете напечатать 
      Найден STACKER m.j

                    Использованная литератрура

 1. Ralf Brown & Jim Kyle
    "PC Interrupts, a programmer's reference to BIOS, DOS, and
    third-party calls".
    /Addison - Wesley Publishing Company,Inc. - 1991
