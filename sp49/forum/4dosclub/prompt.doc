Промпт показывающий
 - время;
 - уровень вложености 4dos;
 - код окончания внешних команд;
 - код окончания внутрених команд 4dos;
 - текущий диск и директорий;
 - издает звукомой сигнал зависящий от кода вытолнемия последней
   команды (т.е. один звук при нулевом коде завершения, другой при не нулевом)

Имеет примерно следующий вид:

18:45 2(0/0) 627k c:\sys >
  |   |  |    |    `-- диск, директорий
  |   |  |    `-- свободной памяти
  |   |  `-- код завершения последней внешней/внутреней команды
  |   `-- уровень вложености 4dos.com
  `-- время


Для его определения необходимо:


в autoexec.bat определить переменую prompt:

prompt=$t$h$h$h  $z(%%?/%%_?) %%@dosmem[k]k $p$g %%@substr[%%@exec[_prompt %%? %%_? %%cmdline],0,0]


определить алиасы:

_prompt       iff "%3" ne "int" then^_prompt_beep %1^else^_prompt_beep %2^endiff^set lec=%1^set lic=%2^set cmdline=int
_prompt_beep  iff "%1" == "0" then^_ok_beep^else^_err_beep^endiff
_err_beep     beep 300 1 400 1 500 1 600 1
_ok_beep      beep 800 1


К сожелению пока еще несовершенен алгоритм определения типа последней
команды. Поэтому иногда тип гудка может несоответствовать действительности.
