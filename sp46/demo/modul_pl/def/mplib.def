(****************************************************************************)
(*                        MPLIB.DEF 2.00                                    *)
(*                    Modula Tools Plus 2.00                                *)
(*                  Copyright (c) Holofax 1992.                             *)
(*              All rights reserved. Ukraine. Kiev.                         *)
(****************************************************************************)
(*$G+*)
DEFINITION MODULE MpLib;
(*╔════════════════════════════════════════════════════════════════════════╗*)
(*║                      Общая библиотека.                                 ║*)
(*╚════════════════════════════════════════════════════════════════════════╝*)

PROCEDURE SRLStrBit(ps : ADDRESS ; l,n : CARDINAL ) ;
(* Сдвигает строку бит, начинающуюся по адресу ps и длиной l-байт, влево на   *)
(* n-бит, причем 0 <= n <= 8, слева - усекаются, а справа - добавляются 0.    *)
(* -------------------------------------------------------------------------- *)

PROCEDURE SRRStrBit(ps : ADDRESS ; l,n : CARDINAL ) ;
(* Сдвигает строку бит, начинающуюся по адресу ps и длиной l-байт, вправо на  *)
(* n-бит, причем 0 <= n <= 8, справа - усекаются, а слева - добавляются 0.    *)
(* -------------------------------------------------------------------------- *)

PROCEDURE LongSRLStrBit(ps : ADDRESS ; l,n : CARDINAL ) ;
(* Сдвигает строку бит, начинающуюся по адресу ps и длиной l-байт, влево на   *)
(* n-бит, причем n произвольное, слева - усекаются, а справа - добавляются 0. *)
(* -------------------------------------------------------------------------- *)

PROCEDURE LongSRRStrBit(ps : ADDRESS ; l,n : CARDINAL ) ;
(* Сдвигает строку бит, начинающуюся по адресу ps и длиной l-байт, вправо на  *)
(* n-бит, причем n произвольное, справа - усекаются, а слева - добавляются 0. *)
(* -------------------------------------------------------------------------- *)

PROCEDURE RoundINC(dig,inc,n : CARDINAL) : CARDINAL  ;
(* Инкремент dig на величину inc, циклически, до n , а потом 0 и т.д.         *)
(* -------------------------------------------------------------------------- *)

PROCEDURE RoundDEC(dig,dec,n : CARDINAL) : CARDINAL  ;
(* Декремент dig на величину inc, циклически, до 0 , а потом n и т.д.         *)
(* -------------------------------------------------------------------------- *)

PROCEDURE IndexMin(n : CARDINAL ; p : ADDRESS) : CARDINAL  ;
(* Ищет индекс минимального cardinalа из n по адресу р                        *)
(* -------------------------------------------------------------------------- *)

PROCEDURE IndexMax(n : CARDINAL ; p : ADDRESS) : CARDINAL  ;
(* Ищет индекс максимального cardinalа из n по адресу р                       *)
(* -------------------------------------------------------------------------- *)

PROCEDURE CutLine(VAR x1,y1,x2,y2 : INTEGER; l,t,r,b : INTEGER ) : BOOLEAN;
(* Определяет отрезок, входящий в прямоугольник l,t,r,b, и возвращает в x1,y1,*)
(* x2,y2 его координаты.Если отрезок не входит в ltrb, возвращается     FALSE *)
(* -------------------------------------------------------------------------- *)

PROCEDURE DiskFreeSpace(driver : CARDINAL) : LONGCARD;
(* Количество свободного места на диске в байтах.                           *)
(*--------------------------------------------------------------------------*)

PROCEDURE CurrentDisk() : SHORTCARD;
(* Текущий диск.                                                            *)
(*--------------------------------------------------------------------------*)

END MpLib.
