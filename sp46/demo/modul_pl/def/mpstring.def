(****************************************************************************)
(*                      MPSTRING.DEF 2.00                                   *)
(*                    Modula Plus Tools 2.00                                *)
(*                  Copyright (c) Holofax 1992.                             *)
(*              All rights reserved. Ukraine. Kiev.                         *)
(****************************************************************************)

(*$G+*)
DEFINITION MODULE MpString;

(*╔════════════════════════════════════════════════════════════════════════╗*)
(*║             Модуль обработки строк.                                    ║*)
(*╚════════════════════════════════════════════════════════════════════════╝*)

(*         ******      Функции преобразования.    ******                    *)
(* ════════════════════════════════════════════════════════════════════════ *)

PROCEDURE HexB(B : BYTE; VAR Str : ARRAY OF CHAR) : BOOLEAN;
(*        Преобразование байта в строку в шестнадцатеричном виде.           *)
(*--------------------------------------------------------------------------*)

PROCEDURE HexW(W : WORD; VAR Str : ARRAY OF CHAR) : BOOLEAN;
(*        Преобразование слова в строку в шестнадцатеричном виде.           *)
(*--------------------------------------------------------------------------*)

PROCEDURE HexL(L : LONGWORD; VAR Str : ARRAY OF CHAR) : BOOLEAN;
(*        Преобразование двойного слова в строку в шестнадцатеричном виде.  *)
(*--------------------------------------------------------------------------*)

PROCEDURE HexPtr(P : ADDRESS; VAR Str : ARRAY OF CHAR) : BOOLEAN;
(*        Преобразование адреса в строку в шестнадцатеричном виде.          *)
(*--------------------------------------------------------------------------*)

PROCEDURE DecW(W : WORD; VAR Str : ARRAY OF CHAR; Pl : BYTE) : BOOLEAN;
(* Преобразование слова в строку в десятичном виде.Pl-количество позиций.   *)
(*--------------------------------------------------------------------------*)


(*               Форматирование строки.                                     *)
(* ════════════════════════════════════════════════════════════════════════ *)

PROCEDURE CharStr(Ch : CHAR; Len : CARDINAL; VAR Str : ARRAY OF CHAR);
(*        Заполнение строки символом.                                       *)
(*--------------------------------------------------------------------------*)

PROCEDURE FormRightCh(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR;
                      Ch : CHAR; Len : CARDINAL);
(*        Форматирует вправо строку с заполнением определенным символом.    *)
(*--------------------------------------------------------------------------*)

PROCEDURE FormRight(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR;
                                                    Len : CARDINAL);
(*        Форматирует вправо строку.                                        *)
(*--------------------------------------------------------------------------*)

PROCEDURE FormLeftCh(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR;
                                                  Ch : CHAR; Len : CARDINAL);
(*        Форматирует влево строку с заполнением определенным символом      *)
(*--------------------------------------------------------------------------*)

PROCEDURE FormLeft(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR;
                                                   Len : CARDINAL);
(*        Форматирует влево строку.                                         *)
(*--------------------------------------------------------------------------*)

PROCEDURE DelSpaceRight(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR);
(*        Убирает пробелы справа.                                           *)
(*--------------------------------------------------------------------------*)

PROCEDURE DelSpaceLeft(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR);
(*        Убирает пробелы слева.                                            *)
(*--------------------------------------------------------------------------*)

PROCEDURE Trim(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR);
(*        Убирает пробелы и слева и справа                                  *)
(*--------------------------------------------------------------------------*)

PROCEDURE FormCenterCh(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR;
                                 Ch : CHAR; Width : CARDINAL);
(*        Центрирует строку с заполнением.                                  *)
(*--------------------------------------------------------------------------*)

PROCEDURE FormCenter(VAR FStr : ARRAY OF CHAR; Str : ARRAY OF CHAR;
                                                     Width : CARDINAL);
(*        Центрирует строку Str в поле Width.                               *)
(*--------------------------------------------------------------------------*)

PROCEDURE CtrlChar(VAR str : ARRAY OF CHAR; left : CHAR) : CHAR;
(*  Возращает первый символ после left                                      *)
(* ------------------------------------------------------------------------ *)

(*               Изменение регистров.                                       *)
(* ════════════════════════════════════════════════════════════════════════ *)


END MpString.
