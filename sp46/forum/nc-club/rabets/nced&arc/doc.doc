                         В.С. Рабец
                         
    РАСШИРЕНИЕ ФУНКЦИИ "external editor" NORTON COMMANDER'а
                    и другие УДОБНЫЕ МЕЛОЧИ

В СП 42 был опубликован материал А.Л. Шаргородского "Диспетчер
редакторов для Norton Commander" (FORUM\DOS-CLUB, файлы NCedit.*
и ext.*), где в качестве внешнего редактора NC предлагается
средство, выполняющее различные действия в зависимости от рас-
ширения обрабатываемого файла. Я предлагаю средство для абсо-
лютно тех же целей и дающее аналогичные результаты, но реализо-
ванное немного проще - диспетчеризация (используя термин
Шаргородского) проводится только средствами NC и встроенных ко-
манд DOS, без специальной внешней EXE-программы (а значит,
быстрее и с экономией дискового пространства).
    Т.к. я писал это средство в основном для беззаботной работы
с архиваторами, то попутно здесь будут приведены еще несколько
файлов на тему архивации.

ГЛАВА 1. ВНЕШНИЙ РЕДАКТОР NC.

      Для работы с внешним редактором нужно указать строку его
      вызова в Options/Editor (F9 O E). Если там выбран
      External редактор, то внешний редактор будет вызываться
      по F4, а встроенный - по Alt-F4; а если выбран Built-in -
      наоборот. Приведенный здесь файл NC.ini содержит необхо-
      димую запись для вызова встроенного редактора по F4 и
      внешнего - по Alt-F4.

В качестве внешнего редактора используется EE.bat (External Editor).
КАК АНАЛИЗИРУЕТСЯ РАСШИРЕНИЕ В bat-ФАЙЛЕ: Параметры в external
editor из NC передаются по правилам NC.ext: знаками !: !\ ! !.!
Т.е. можно передать что угодно - диск, маршрут, имя файла с
расширением и без, но только не отдельно расширение. Однако
можно определить расширение, сравнивая имя_файла_с_расширением
и имя_файла_без_него. Если в строке вызова external editor за-
писать
                          ee  !.!  !
то в EE.bat параметр %1 будет соответствовать файлу с расшире-
нием, а %2 - без него, тогда расширение проверяется так:
                     if .%1.==.%2.zip. ...
и т.д.

    Файл EE.bat приведен ниже, для иллюстрации строки пронуме-
рованы. Работает он следующим образом: если расширение обраба-
тываемого файла архивное (.ARJ .LZH .ZIP .ICE .PAK .ARC), то
вызывается соответствующий разархиватор, иначе - предполагает-
ся, что файл текстовой, и вызывается Lexicon. Разархивация (с
сохранением атрибутов и дерева каталогов) задана в строках 3-8.
В случае успешного завершения выдается яркая надпись "OK!"
(стр. 19), иначе (заполнен диск и т.п.) - гудок и яркая мигаю-
щая надпись "Error" (стр. 9) с паузой (стр. 10) (яркость и ми-
гание будут только при установленном ANSI.SYS). Строка 11 - ес-
ли расширение не архивное, то перехода нет, и выполняются сле-
дующие строки - вызов Lexicon'а. Строка 12 - отключение NumLock
(Leeman, Podolyak, Kohmanyuk, Sviridov. Софтпанорама 24).

    Загрузка Lexicon'а при вызове EE.bat как внешнего редактора
NC происходит в строках 15 (если файл с расширением) и 16 (у
файла нет расширения).
      Назначение строк 13-14 и 17: Вы можете не указывать
      Lexicon в PATH (во-первых, ко всем пакетам PATH не ука-
      жешь, во-вторых - Безруков рекомендует минимизировать
      PATH из антивирусных соображений), а в случае необходи-
      мости вызова Lexicon'а в произвольном директории набрать
                EE  filename1 filename2...           {1}
      или       EE  filename                         {2}
      Вызов {2} обслуживается строками 13-14, {1} - строкой 17.

 1  @echo off
 2  echo V.S. Rabets  14-03-91
 3  if .%1.==.%2.zip. pkUnzip -d -JHSR %1
 4  if .%1.==.%2.lzh. LHA x /a /i1 /l1 %1
 5  if .%1.==.%2.arj. ARJ x -y -- %1
 6  if .%1.==.%2.ice. lhArc x /r /m %1
 7  if .%1.==.%2.arc. unPack %1
 8  if .%1.==.%2.pak. Pak e /Path %1
 9  if errorlevel 1 echo         [1;5mError[0m
10  if errorlevel 1 pause
11  for %%e in(zip lzh arj ice arc pak) do if .%1.==.%2.%%e. goto Quit
12  nloff
13  if .%2.==..  D:\EDIT\LEX\lex %1
14  if .%2.==..  goto Quit
15  if not exist %2  D:\EDIT\LEX\lex %1
16  if     exist %2 if     %1.==%2.  D:\EDIT\LEX\lex %1
17  if     exist %2 if not %1.==%2.  D:\EDIT\LEX\lex %1 %2 %3 %4 %5 %6 %7 %8 %9
18  :Quit
19  if not errorlevel 1 echo [1m  OK![0m

    Попутно еще 2 слова о вызове Lexicon'а. Если Вы предполага-
ете использовать EE.bat таким образом (для вызова Lexicon'а, к
которому не указан PATH) и у Вас есть небольшой RAM-диск, то
лучше RAM-диск поставить первым в PATH, а в AUTOEXEC копировать
на RAM-диск файлы SetUp.lex и KeyMacro.lex (они маленькие и не
сильно там помешают), тогда Lexicon будет находить их мгновен-
но, а не долго рыскать по PATH в бесплодных их поисках.

ПОДВЕДЕМ ИТОГИ: впишите в Options/Editor строку
                         ee !.! !
и укажите Built-in. Тогда при нажатии F4 всегда будет вызывать-
ся встроенный редактор, а при нажатии Alt-F4 - архивы будут
разархивироваться, независимо от их типа, с сохранением атрибу-
тов и дерева, а другие файлы - загружаться в Lexicon.
    Это особенно удобно когда, например, к Вам вдруг попадает
какой-нибудь .ARC или .PAK файл, которых Вы уже 100 лет не ви-
дели и забыли не только опции разархивации, но и точное назва-
ние самого архиватора.
    ДЕФЕКТЫ EE.bat: если в каталоге есть два файла с одинаковым
основным именем, но один из них без расширения, например,
somefile.ext и somefile, то при редактировании somefile.ext по
Alt-F4 во второе окно Lexicon'а загружается somefile.


ГЛАВА 2. ФАЙЛЫ, СОПУТСТВУЮЩИЕ EE.bat.

NC.EXT:
    Файл NC.ext можно задействовать для просмотра содержания
архивов. Это лучше, чем задавать в NC.ext распаковку архива,
потому что Enter легко нажать случайно.
    Попутно о возможности выполнения нескольких команд для одно-
го расширения. Предположим, Вы хотите вызывать все тот же
Lexicon по расширению TXT. У Lexicon'а есть мерзкая особенность
- при включенном NumLock клавиши перемещения курсора (в т.ч. и
не совмещенные с цифрами) работают как цифровые. Поэтому было
бы удобно сначала запускать NLoff. Однако каждому расширению
можно поставить в соответствие только одну команду. Выходов два:
    1) FOR: Если обе команды без параметров, или
                 их параметры одинаковы, или
                 одной из них параметры безразличны -
            можно организовать цикл;
    2) КОНВЕЙЕР: Если первая команда не производит способного к
           перенаправлению вывода, а вторая - такого же ввода, -
           можно организовать конвейер. (Не забывайте, что под
           Command.com'ом он не будет работать на защищенном от
           записи диске. Впрочем, едва ли много смысла вызывать
           на таком диске редактор).
В нашем примере оба варианта приемлемы:
    TXT:  for %c in(NLoff D:\EDIT\LEX\lex) do %c !.!
    TXT:  NLoff | D:\EDIT\LEX\lex !.!

NC.MNU:
    В NC.mnu удобно вставить распаковку всех архивов текущего
директория. В приведенном здесь файле этой функции соответству-
ет кнопка F7.

BAT-ФАЙЛЫ:
    Файлы ar.bat, lzh.bat, zip.bat архивируют соответствующим
архиватором текущий директорий с поддиректориями, сохраняя ат-
рибуты файлов и дерево каталогов (AR.bat, к сожалению, нельзя
назвать ARJ, потому что ARJ - не только расширение, но и имя
самого архиватора). В качестве параметра указывается имя файла-
результата, например:
      AR newarc
 или  AR newarc.arj
- весь текущий директорий с поддиректориями упаковывается в
файл newarc.arj. Ниже приведен ZIP.BAT, строки для иллюстрации
пронумерованы:

1  rem  V.S. Rabets  18-12-90
2  pkzip -r -P -wHS %1 %2 %3 %4 %5 %6 %7 %8 %9
3  @if errorlevel 1 echo        [1;5mError[0m
4  @if errorlevel 1 pause
5  @if not errorlevel 1 echo [1m  OK![0m

Архивация производится строкой 2, при успешном завершении выда-
ется яркая надпись "OK!" (строка 5), иначе - гудок и яркая ми-
гающая надпись "Error" (строка 3) с паузой (строка 4).

    Файл SFX.BAT - создание самораспаковывающегося архива LHA,
например SFX newarc.lzh .
    В файле Double.lzh запакованы bat-файлы, дублирующие функ-
ции работы с архивами вышерассмотренных EE.bat и NC.ext.
