* VIEW FILE IN BINARY MODE
	Welcome to Binary View! The file is shown just as it lies on disk, no
characters are treated specially.

			       ^BNavigation Keys^b

	^B<UP>,<DN>,<PGUP>,<PGDN>^b -- Scroll through the file
	^B<HOME>^b -- Go to start of file
	^B<END>^b -- Go to end of file
	^B<Ctrl>^b, or click mouse -- Move the current character indicator
around the screen. The hex value of that character is displayed in the upper
right corner of the screen.

				 ^BOther keys^b

	^B<CtrlG>^ -- @AUGMENTA.HLP^*%Change_Font[ Change screen font. ]
	^B<F10> or <ESC>^b -- Quit Binary View, return to DOS Shell.
SEARCH ASCII SEARCH
	Input the search string and press ^B<CR>^b. Use ^B<CtrlF6>^b to continue
searching.
HEXSRC HEX SEARCH
	Type hex values of characters you wish to look for. Press ^B<CR>^b to
confirm or ^B<ESC>^b to abort operation.
LSEEK LSEEK
	To perform Lseek, fill in the bytes count in hex or dec (you must
specify which value is to be used), and select ^BOK^b to perform the operation.
