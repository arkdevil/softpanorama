/* void initfile(dict) */
void initfile();

/* void closefile() */
void closefile();

/* void buildindex() */
void buildindex();

/* int readindex(indx) */
int readindex();

/* void writeindex(indx) */
void writeindex();

/* int searchfile(match, entry) */
int searchfile();

#define FAIL (0)
#define SUCCESS (1)
