CB
                   EПРОГРАММА КОНТРОЛЯ СОХРАННОСТИ ФАЙЛОВF


                                                          EГонтарь Б.Б.F
                                                   ИНТЕРКВАДРО, г.Киев


          Если Вы  некоторое время  не изменяли  файлы на своем диске,
     это еще  не  значит,  что  они  остались  такими  же,  как  были.
     Возможные причины  порчи  файлов:  пакости  со  стороны  вирусов,
     работа на  персональном компьютере в режиме коллективного пользо-
     вания, запуски не вполне отлаженных программ.

          Программа  EcheckF   предназначена  для  контроля  сохранности
     файлов. В каждом справочнике, который подлежит контролю, она соз-
     дает файл  с именем Echeck.sumF, в котором о каждом файле  хранится
     часть информации  из справочника:  имя  файла,  время  последнего
     изменения, размер файла, а также добавляется контрольная сумма.

          Программа вызывается командной строкой:

                      check [ флаги ] [ справочники ]

          Простейший вариант  вызова EcheckF  контролирует текущий спра-
     вочник. Вызов  с флагом E-rF контролирует также все его подсправоч-
     ники, их  подсправочники и  т.п. Вызов  с флагом E-cF создает файлы
     "check.sum" в  тех справочниках, где их нет; при вызове без этого
     флага такие справочники пропускаются. Флаг E-vF заставляет програм-
     му выдавать сообщения, без которых можно обойтись. Кроме флагов в
     командной строке  можно перечислить проверяемые справочники и/или
     устройства (с двоеточием, напр. "a:").

          Логика работы программы следующая. Просматривается очередной
     справочник, и, если в нем обнаружен "новый" (т.е. отсутствовавший
     в файле  "check.sum") файл,  информация о  нем  вносится  в  файл
     "check.sum". Это  делается молча,  если данный  файл моложе,  чем
     "check.sum", или в присутствии флага E-cF; иначе выдается сообщение
     и запрос. Для "старых" файлов делаются две проверки. Если файл не
     изменился ни  в размере,  ни в контрольной сумме, проверяется, не
     изменился ли  его возраст;  если же  файл изменился, проверяется,
     действительно ли  он стал  моложе. Затем  файлы,  исчезнувшие  из
     справочника, удаляются  из "check.sum". Далее, при указании флага
     E-rF, программа спускается в подсправочники.

          Естественные изменения  в справочнике (файл изменился и стал
     моложе; файл  создан позже, чем "check.sum"; файл  уничтожен) ре-
     гистрируются в  "check.sum". Поэтому  в тех справочниках, которые
     не должны  изменяться, следует,  создав файл "check.sum", устано-
     вить для него запрет записи (напр., командой attrib).

