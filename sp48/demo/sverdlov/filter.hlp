       Предлагаемые варианты сглаживания:
 - медианный фильтр - для каждых трех соседних
 точек  сигнала  выбирается  та, которая имеет
 среднее значение;
 - "идеальный" фильтр -  выполняется   свертка
 сигнала с функцией вида:
   SIN (Pi * Fсреза * X)/(Pi * X) ,
 где Fсреза - относительная частота сpеза,
     Pi=3.14159265,
     -31 < X < 31 (номера точек);
 - эвристический фильтр  -    модифицированный
 "идеальный"  с  сохранением   формы   фронтов
 сигнала.
