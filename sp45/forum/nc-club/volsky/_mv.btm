@echo off
IFF  isdir %1  then
     iff %@substr[%@full[%1],0,2]==%@substr[%@full[%2],0,2]   then
          ncd graft %&
     else
          ncd copy /delete %&
     endiff
ELSE
     move %&
ENDIFF
