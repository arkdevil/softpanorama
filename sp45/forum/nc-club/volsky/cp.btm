@echo off
:
:    The same as MV, but not delete source. See also MV.BTM comment.
:
iff  "%2"==""  then
     iff "%1"=="" then
          nca _CP ~{ } ~T
     else
          nca _CP %1 ~T
     endiff
else
              _CP %&
endiff

