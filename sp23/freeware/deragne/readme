EXAMPLES
1.
runtree @erase big_dir -i*.bak -eread

	In directory big_dir and it's sub-directores all files *.bak
	whill be erased, excluding files, which names contain 'read' as
	sub-sring (READme.BAK , fullREAD.BAK , etc.).

2.
runtree @pack a:\ b:\body -usoft_pan

	In directory b:\body whill contain:
		a). File soft_pan.lst. This file contain strings :
			"SubDir A:\ Pack to Archive SOFT_0D6.ARC"
					.
					.
			"SubDir A:\MARKET Pack to Archive SOFT_A00.ARC"
		b). All archive files listed in '.lst'-file.

	Note.   a). Dir b:\body must exist before call RunTree;
		b). name of '.lst'-file (or '.bat'-file if using '-b' option)
			cannot contain a PATH and EXTENSION;
		c). all archives names will be unique in b:\body;
		d). if options '-b' and '-u' not using,RunTree asking 
			archives names from stdin (usially console).
		e). option '-i' may using whith "@Pack", but '-e'  cannot.

3.
runtree @copy a:\graf  c:\grafics  -a  -i*.pic -i*.scr -i*.pal -ebadpic

	If diskette A: contain a tree of sub_dir, such as:

	A:\ --+----> dbase
              +----> foxbase
	      +----> graf --+----> pictures
			    +----> old_pict


	then :
		a). RunTree whill asking for all sub-dir ;
		b). RunTree whill asking for all files, which it may
			copy : *.pic , *.scr , *.pal excluding 'badpic'-files;
		c). sub-dir c:\grafics , c:\grafics\pictures and
			 c:\grafics\old_pic will create, if thay not
			 exist and if user answer 'Y' on asking about
			this sub-dir;






